#pragma once

#include "ui.h"

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

struct _e_weight_list : public _e_scrollable_area
{
	int font_size = 16; // минимум 12 для читабельности
	std::vector<std::wstring> strings;
	_e_weight_list(_ui* ui_);
	void draw(_trans tr) override;
	void update() override;
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
